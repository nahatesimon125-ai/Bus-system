{
  "name": "nts-regional-bus-tickets-django",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node start-django.js",
    "build": "node prepare-django.js",
    "start": "node start-django.js",
    "clean": "rm -f db.sqlite3 && rm -rf tickets/migrations/*",
    "lint": "echo 'Skipping node linting since we are running Django only'"
  },
  "dependencies": {}
}
